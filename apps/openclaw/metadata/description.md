# OpenClaw

OpenClaw is an open-source coding agent you can run in your own infrastructure.

This RunTipi package deploys OpenClaw with:

- Persistent config and workspace storage
- Domain-aware frontend URL configuration for reverse proxy access
- Docker socket integration for sandbox tool execution

To start using models, provide at least one API key (OpenAI, Anthropic, or OpenRouter) in the app settings.
